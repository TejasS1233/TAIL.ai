# ==============================================
# BACKEND API CONFIGURATION
# ==============================================
# Base URL for the backend API
# This should point to your Node.js server
VITE_API_BASE_URL=http://localhost:8000/api/v1

# WebSocket URL for real-time features
# Should match your server's WebSocket configuration
VITE_SOCKET_URL=http://localhost:8000

# Flask API URL (if you have a separate Flask service)
# Comment out if not using Flask
VITE_FLASK_API_URL=http://localhost:5000

# ==============================================
# APPLICATION CONFIGURATION
# ==============================================
# Application name (displayed in UI)
VITE_APP_NAME=TAIL.AI

# Application version
VITE_APP_VERSION=1.0.0

# Environment (development, staging, production)
VITE_ENV=development

# ==============================================
# FEATURE FLAGS (Optional)
# ==============================================
# Enable/disable features during development
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG_MODE=true
VITE_ENABLE_MOCK_DATA=false

# ==============================================
# NOTES FOR VITE ENVIRONMENT VARIABLES
# ==============================================
# 1. All Vite env variables MUST start with VITE_
# 2. Variables are embedded at build time (not runtime)
# 3. Never store sensitive data here (API keys, secrets, etc.)
# 4. These values are exposed to the browser/client
# 5. Changes require rebuild: stop dev server and run `npm run dev` again
# 
# Example usage in code:
#   const apiUrl = import.meta.env.VITE_API_BASE_URL;
#   const appName = import.meta.env.VITE_APP_NAME;